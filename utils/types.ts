export type Article = {
    author: string,
    categories: string[],
    date: Date,
    featured: boolean,
    image: string,
    title: string,
    subtitle: string,
    slug: string,
    readingTime: string

}